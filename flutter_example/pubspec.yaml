name: flutter_example
description: "image_pipeline flutter exmaple"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.11.5
dependencies:
  flutter:
    sdk: flutter
  cross_file: ^0.3.4
  file_picker: ^8.0.0
  share_plus: ^10.0.0
  image_pipeline:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
