[x] API documentation
[x] benchmark, benchmarks vs other libries
[x] README to explain how the build works, packages used for each platform, how to add new operations, how to use the library
[x] Nella ci devono essere molte più architetture (Linux x64/arm64/ia32, macOS x64/arm64, Windows x64/arm64)
[x] migliorare builds per macOs e fix runners
[ ] CI tests (unit tests in CI)
[ ] action for test: 1. setup test downloading the assets for the specific version then: 2. lib tests for dart, lib test --chrome for web, web_integration to ensure native then upload everything to codecov
[ ] nel readme spiegare meglio tutte le build che faccio, il fatto che se non c'è online te la fa in locale e se c'è l'hai online usi le build più performanti